input {
	tcp {
		port => 5000
	}
	udp {
		port => 12201
	}
}

## Add your filters / logstash plugins configuration here
filter {
    json {
        source => "message"
#        target => "jsoncontent"
    }
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
        index => "transfer-%{+YYYY.MM.dd}"
	}
}
